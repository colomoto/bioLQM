/**
 * Manage features added to the core logical model data structure.
 * 
 * A simple {@link org.colomoto.biolqm.services.ServiceManager} maintains a list of
 * available services (only formats for now).
 * These services can then be called from a command-line interface.
 */
package org.colomoto.biolqm.services;
